# Copy to `.env.prod` — PRODUCTION environment (git branch `main`).
# Do not commit `.env.prod`.
# Used locally when on branch `main`; mirror these values in Vercel Production scope.
# Supabase project: uccivcdtfpevtykirkuw
# See docs/ENVIRONMENTS.md

# ── Required ──────────────────────────────────────────────────────────────────
# Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://uccivcdtfpevtykirkuw.supabase.co

# Dashboard → Project Settings → API → anon public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-main-anon-key

# Dashboard → Project Settings → Database → Database password
SUPABASE_DB_PASSWORD=your-main-db-password

# ── Optional — production canonical URL (for local prod smoke tests) ────────
# NEXT_PUBLIC_SITE_URL=https://your-production-domain.ro

# Do NOT set NEXT_PUBLIC_TITLE_PREFIX here.
